import Blazy from 'blazy';

window.onload = function () {
    /**
     * bLazy trigger
     */
    let picwall = document.getElementById('picwall_container');
    if (picwall != null) {
        let bLazy = new Blazy();
    }
};
